// import { datasets, datasetLabels } from "./data";
// import DatasetDisplay from "../DatasetDisplay"

export default function Home({ params: { name } }) {
  return (
    <p>TODO: npm build error with name {name}</p>
    // <DatasetDisplay
    //   title={`${name}的数据集`}
    //   datasets={datasets}
    //   datasetLabels={datasetLabels}
    //   isvisitor={true}
    // />
  );
}
